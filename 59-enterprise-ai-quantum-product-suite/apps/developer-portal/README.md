# Developer portal contract

The API's generated Swagger UI (`/docs`) is the reference developer portal for this technical candidate. It exposes versioned operations and examples without duplicating the enterprise decision UI. A separately deployed developer experience is a production gap, not a hidden second application.

