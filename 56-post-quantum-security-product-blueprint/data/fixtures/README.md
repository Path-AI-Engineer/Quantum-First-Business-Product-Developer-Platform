# Safe offline connector fixtures

All IDs and labels are fictional. These files exercise each parser format;
the full 125/220/35/28 corpus is generated deterministically by
`src/pqc_product/corpus.py`. Fixtures never contain private keys, real
certificates, PII, network targets or customer identifiers. The known-finding
IDs are development fixtures, **not** an unopened blinded final test set.
