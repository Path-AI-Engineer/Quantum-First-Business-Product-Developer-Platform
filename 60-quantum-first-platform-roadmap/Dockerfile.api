FROM python:3.13-slim
WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
RUN python -m pip install --no-cache-dir .
EXPOSE 8060
CMD ["python", "-m", "uvicorn", "strategy_control_tower.api:app", "--host", "0.0.0.0", "--port", "8060"]

