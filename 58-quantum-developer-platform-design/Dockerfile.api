FROM python:3.13-slim
WORKDIR /app
COPY pyproject.toml ./
COPY src ./src
RUN python -m pip install --no-cache-dir .
EXPOSE 8058
CMD ["python", "-m", "uvicorn", "quantum_platform.api:app", "--host", "0.0.0.0", "--port", "8058"]

